@{
    ViewBag.Title = "Home Page";
}
@section featured {
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <h1>@ViewBag.Title.</h1>
                <h2>@ViewBag.Message</h2>
            </hgroup>
            <p>
                To learn more about ASP.NET MVC visit
                <a href="http://asp.net/mvc" title="ASP.NET MVC Website">http://asp.net/mvc</a>.
                The page features <mark>videos, tutorials, and samples</mark> to help you get the most from ASP.NET MVC.
                If you have any questions about ASP.NET MVC visit
                <a href="http://forums.asp.net/1146.aspx/1?MVC" title="ASP.NET MVC Forum">our forums</a>.
            </p>
        </div>
    </section>
}
<h3>We suggest the following:</h3>
<ol class="round">
    <li class="one">
        <h5>Getting Started</h5>
        "View source" to find the script running.
    </li>

    <li class="two">
        <h5>Add NuGet packages and jump-start your coding</h5>
        <code>corsproxy.aspnet</code> and <code>corsproxy</code>
    </li>

    <li class="three">
        <h5>Follo me</h5>
        <a href="http://blog.gauffin.org">blog.gauffin.org</a>
    </li>
</ol>
<h2>Todo</h2>
<div ng-controller="TodoListController as todoList">
    <span>{{todoList.remaining()}} of {{todoList.todos.length}} remaining</span>
    [ <a href="" ng-click="todoList.archive()">archive</a> ]
    <ul class="unstyled">
        <li ng-repeat="todo in todoList.todos">
            <input type="checkbox" ng-model="todo.Done">
            <span class="done-{{todo.Done}}">{{todo.Text}}</span>
        </li>
    </ul>
    <form ng-submit="todoList.addTodo()">
        <input type="text" ng-model="todoList.todoText" size="30"
               placeholder="add new todo here">
        <input class="btn-primary" type="submit" value="add">
    </form>
</div>
|@section scripts
 {
     <script type="text/javascript">
         $(function() {
             $.ajax({
                 url: "http://localhost:54160/values/1",
                 type: 'GET',
                 dataType: 'json',
                 crossDomain: true,
                 data: '22',
                 success: function(result) {
                     console.log(result);
                 },
                 error: function (request, status, error) {
                     console.log(request.responseText);
                     console.log(status);
                     console.log(error);
                 }

             });

             //forbidden as we have added an allowed host
             $.ajax({
                 url: "http://somewhere.com/values/1",
                 type: 'GET',
                 dataType: 'json',
                 crossDomain: true,
                 data: '22',
                 success: function (result) {
                     console.log(result);
                 },
                 error: function (request, status, error) {
                     console.log(request.responseText);
                     console.log(status);
                     console.log(error);
                 }

             });
       
     });
     </script>
 }
